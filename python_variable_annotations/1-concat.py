#!/usr/bin/env python3
"""Write a type-annotated function concat that takes two strings as arguments
and returns their concatenation.
"""


def concat(str1: str, str2: str) -> str:
    """Returns a concatenated string."""
    return str1 + str2
